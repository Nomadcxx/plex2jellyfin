# Web & Config Ponytail Audit

## Findings

| Rank | File:Line | What | Why | Fix | Lines Saved |
|------|-----------|------|-----|-----|-------------|
| **1** | `internal/config/config.go:340-540` | `ToTOML()` is a 200-line hand-rolled TOML serializer using `fmt.Sprintf` with 50+ positional args | Viper (already imported) has `WriteConfig()` and `WriteConfigAs()`. This is reinventing TOML serialization. Every new config field requires updating the format string in the right position — fragile and error-prone. | Replace with `v.SetConfigFile(path); v.WriteConfig()` or use BurntSushi/toml (already a transitive dep via viper). The `Save()` method becomes: marshal struct with toml library, then `AtomicWriteWithLock`. | ~180 |
| **2** | `internal/config/config.go:310-340` | `DefaultAIConfig()` duplicates AI defaults already in `DefaultConfig()` | Two sources of truth with different values: `TimeoutSeconds` is 5 here vs 30 in `DefaultConfig()`, `RetryDelay` is 100ms vs 500ms. Only one caller found (`DefaultAIConfig` unused in this audit sample). | Delete `DefaultAIConfig()`. If needed, extract from `DefaultConfig().AI`. | ~30 |
| **3** | `internal/config/sections.go:70-130` | 11 identical `set*` functions (`setWatch`, `setLibraries`, `setSonarr`, ...) — each is the same 4-line pattern: `decodeSection(raw, &v); c.X = v; return nil` | Copy-paste repeated 11 times. Adding a new config section requires another copy. | Replace with a single generic `setSection[T any]` function using a type parameter, or a table-driven approach with `reflect` setting the field by name. The `sections` map already has the type info via the `get` closures. | ~55 |
| **4** | `internal/api/sse_relay.go:15-18` | `IPCAttacher` interface with exactly one implementation (`*ipc.Client`) | Classic "interface with one implementation" anti-pattern. The comment says "Production wiring is satisfied by *ipc.Client" — that's the only impl. | Delete the interface, use `*ipc.Client` directly. If a test double is needed later, the test can create one then. | ~4 |
| **5** | `internal/api/dashboard.go:12-19` | `DashboardData` struct defined locally but never used — handler uses `api.DashboardData` (generated) instead | Dead code. The local type shadows the OpenAPI-generated type but `GetDashboard` at line 65 uses `api.DashboardData`. | Delete the local `DashboardData` type and its dependent unused types (`LibraryStats`, `MediaManagerSummary`, `LLMProviderSummary`, `ActivityEvent`). | ~40 |
| **6** | `cmd/installer/animations.go:1-771` | 771-line `BeamsTextEffect` — a full custom ASCII animation engine with beam groups, gradient stops, diagonal wipe phases, and per-character state | This is a cosmetic installer header effect. The static ASCII art already exists in `theme.go`. A 771-line animation system for a one-time installer header is the definition of over-engineering. | Delete the entire file. Use the static `asciiHeader` from `theme.go`. The `view.go` fallback path already handles this. | ~771 |
| **7** | `cmd/installer/theme.go:24-28` + `cmd/installer/theme.go:30-33` | `asciiHeaderLines` (slice) and `asciiHeader` (const string) contain the exact same ASCII art duplicated | Two copies of the same 4-line ASCII block. The slice is used by the beams animation (which should be deleted per #6), the const is the fallback. | Delete `asciiHeaderLines` (only used by beams animation). Keep `asciiHeader` const. | ~4 |
| **8** | `cmd/installer/inputs.go:1-296` | 6 nearly identical `init*Inputs`/`save*Inputs` function pairs (`initPathsInputs`, `initSonarrInputs`, `initRadarrInputs`, `initJellyfinInputs`, `initAIInputs`, `initPermissionsInputs`, `initWebInputs`) — each creates `textinput.Model` with the same styling boilerplate repeated | Same pattern: `textinput.New()`, set placeholder/width/charlimit/value, apply 5 identical style lines. Repeated ~30 times across 6 functions. | Extract a `newStyledInput(placeholder, width, charLimit, value, echoMode)` helper. Each init function becomes 1-3 lines per input. | ~200 |
| **9** | `cmd/installer/validation.go:55-160` | `testSonarrCmd`, `testRadarrCmd`, `testJellyfinCmd` are 3 nearly identical HTTP test functions — same pattern: create client, make GET request, set auth header, parse version | Copy-paste with only URL path and auth header format differing. | Single `testServiceCmd(url, apiKey, path, authHeaderBuilder)` function. The 3 callers pass the varying parts. | ~60 |
| **10** | `internal/api/test_handlers.go:45-95` | `Sonarr`, `Radarr`, `Jellyfin` test handlers are 3 nearly identical methods — same pattern as installer validation #9 | Same copy-paste as the installer but in the API layer. Two copies of the same logic exist (installer + API). | Single generic test handler. Also deduplicate with installer's version (#9) — extract shared test logic to a common package. | ~30 |
| **11** | `internal/api/media_managers.go:155-185` | `convertSonarrQueueItems` and `convertRadarrQueueItems` are near-identical converters — same field mapping, different source types | The only difference is the input slice type (`[]sonarr.QueueItem` vs `[]radarr.QueueItem`). Both have identical fields. | If `sonarr.QueueItem` and `radarr.QueueItem` share the same shape, unify them into one type or use a generic converter. Otherwise, a single function with an interface for the source item. | ~20 |
| **12** | `internal/api/ai_custom.go:130-190` + `cmd/installer/validation.go:230-290` | `TestAIPrompt` (API) and `testOllamaPromptCmd` (installer) duplicate the same Ollama prompt test logic — same endpoint, same prompt, same JSON parsing | Two copies of Ollama `/api/generate` call with the same test prompt. Divergence risk: one gets fixed, the other doesn't. | Extract shared Ollama test logic to `internal/ai/` or `internal/config/`. Both callers use it. | ~40 |
| **13** | `cmd/installer/utils.go:55-70` | `detectPackageManager()` returns a string but has zero callers in the audited files | Dead code. Searched tasks.go, main.go, update.go — no usage. | Delete. | ~15 |
| **14** | `cmd/installer/utils.go:130-140` | `resolveInstallerProjectRoot()` is a 3-line wrapper that just calls `resolveProjectRoot(cwd, exe)` | Unnecessary indirection. The wrapper adds no logic. | Inline the call to `resolveProjectRoot` at the two call sites. | ~3 |
| **15** | `internal/api/settings_handlers.go:110-145` | `preserveMaskedSectionSecrets` has a long switch with repeated `isMaskedSecret` + reassignment pattern | Each case does the same thing: check if masked, restore from current config. | Table-driven: map section name to list of `(fieldPtr, currentFieldPtr)` pairs. | ~15 |
| **16** | `internal/config/config.go:70-120` | `ResolveUID` and `ResolveGID` are near-identical — same "try numeric, else lookup" pattern | Copy-paste with `user.Lookup` vs `user.LookupGroup`. | Single `resolveID(raw string, lookup func(string) (*user.User/Group, error), idField func(*user.User/Group) string)` helper. | ~20 |
| **17** | `internal/config/config.go:122-145` | `ParseFileMode` and `ParseDirMode` are identical except for which field they read | Same logic: trim, pad to 3-char octal, parse. | Single `parseMode(raw string) (os.FileMode, error)`. Callers pass `p.FileMode` or `p.DirMode`. | ~10 |
| **18** | `internal/api/handlers.go:380-420` | `generateEventID` uses `uuid.NewMD5(uuid.Nil, ...)` — cryptographic hash for a display-only activity ID | MD5 of UUID namespace + timestamp+action+source is overkill for a UI event ID. | `fmt.Sprintf("%d-%s", entry.Timestamp.UnixNano(), entry.Action)` — unique enough for a scrolling activity feed. | ~3 |
| **19** | `internal/api/auth.go:28-32` | `secureRandomRead = rand.Read` — package-level variable wrapping a stdlib function | Indirection with no benefit. Only used in `generateToken`. | Call `rand.Read` directly. | ~1 |
| **20** | `internal/api/server.go:170-175` | `getConfigDir()` is a 3-line wrapper that just calls `paths.JellyWatchDir()` | Unnecessary indirection. Only called once in `NewServer`. | Inline the call. | ~3 |

## Needs Deeper Research

| # | Finding | Why cross-package |
|---|---------|-------------------|
| **C1** | `ToTOML()` replacement (#1) touches config save pipeline (`config/save.go`, `api/config_save_pipeline.go`, `api/settings_handlers.go`, `api/paths_handlers.go`) — all callers of `Config.Save()` or `AtomicWriteWithLock` with `ToTOML()` output | Verify viper round-trips all fields correctly, especially `PermissionsConfig` conditional block and `PasswordHash` append. The `mapstructure` tags must map 1:1 to TOML keys. |
| **C2** | Ollama test logic duplication (#12) spans `internal/api/ai_custom.go`, `cmd/installer/validation.go`, and possibly `internal/ai/` package | Need to check if `internal/ai/` already has an Ollama client that both could share. If not, extract one. |
| **C3** | `*arr` test connection duplication (#9, #10) spans `internal/api/test_handlers.go`, `cmd/installer/validation.go`, and the underlying `internal/sonarr/`, `internal/radarr/`, `internal/jellyfin/` client packages | The underlying clients already have `GetSystemStatus()`. The test handlers are thin wrappers that could be eliminated if the API just calls the client directly. |
| **C4** | `BeamsTextEffect` deletion (#6) requires checking `cmd/installer/update.go` and `cmd/installer/view.go` for all references to `m.beams`, `m.ticker`, `NewBeamsTextEffect`, `NewTypewriterTicker` | The ticker and beams are intertwined in the update loop and view. Removing animations means also removing the ticker system and simplifying the `Update()` function. |
| **C5** | `ManagerClient` interface (#4 related) — check if `internal/sonarr/Client`, `internal/radarr/Client`, `internal/jellyfin/Client` could implement a shared interface directly instead of via wrappers | The wrappers exist because the underlying clients have different method signatures (`GetSystemStatus` returns different types). A shared interface would require unifying those return types. |
