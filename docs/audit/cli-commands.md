# CLI & Commands Ponytail Audit

## Findings

| Rank | File:Line | What | Why | Fix | Lines Saved |
|------|-----------|------|-----|-----|-------------|
| 1 | `cmd/jellywatch/main.go:1088`, `cmd/jellywatchd/main.go:830`, `cmd/jellywatch/cleanup.go:12` | `isMediaFileCheck` / `isMediaFile` / `videoExtensions` — 3 copies of the same media-extension map | Duplicated across 3 files. Same map, same logic. | Move to `internal/formatter.go` (already has `formatBytes`) or a shared `mediautil` package. One function, one map. | ~30 |
| 2 | `cmd/jellywatch/main.go:400-600` | Sonarr/Radarr command trees — `newSonarrCmd` + `newRadarrCmd` are structurally identical with copy-pasted queue display, clear-stuck, and import logic | ~150 lines of near-identical code. Status/queue/clear/import subcommands differ only in client type and field names. | Single `newArrCmd(name, clientFactory)` that takes a config struct. Queue display loop is identical — extract to `printArrQueue`. | ~80 |
| 3 | `cmd/jellywatch/audit_validation.go:155`, `cmd/jellywatch/scan_progress.go:30`, `cmd/jellywatch/audit_cmd.go` (implicit) | 3 separate `ProgressBar` implementations — `ProgressBar` (audit_validation), `ScanProgressBar` (scan_progress), and inline progress in audit_cmd | Same concept: total/current/rendered bar. Three implementations with different field names and render logic. | One `ProgressBar` in `internal/progress/` with `New(total, unit)` and `Update(current, extra...)`. | ~100 |
| 4 | `cmd/jellywatch/audit_preview.go:1-210` | TUI preview using charmbracelet/bubbles (viewport, tea, lipgloss) for audit dry-run | Heavy dependency for a CLI preview. Same info could be printed to stdout with `less`-style pagination or plain text. | Replace with `fmt.Print` + `--show-all` flag. If scrolling is essential, pipe to `less` via `os/exec`. Drop bubbles + tea + lipgloss from this file. | ~210 |
| 5 | `cmd/jellywatch/status_cmd.go:200-270` | `deploymentDriftWarnings` + `printDeploymentDrift` — SHA256 comparison of `/tmp/jellywatch*` vs `/usr/local/bin/jellywatch*` | Deployment drift detection in a status command. Hardcoded paths. This is a CI/deploy concern, not a runtime status concern. | Delete. If needed, a `make deploy-check` target or post-build script. | ~70 |
| 6 | `cmd/jellywatch/audit_cmd.go:100-108` | `dbProvider` struct — implements `ai.DatabaseProvider` interface with one method | Interface with exactly one implementation. The method just returns `p.mediaDB.DB()`. | Inline: pass `*sql.DB` directly instead of wrapping in interface+struct. | ~8 |
| 7 | `cmd/jellywatchd/streaming_ops.go:120-125` | `aiBatcher` interface — one method, one implementation | Interface with one implementation. `MediaHandler` already has the method. | Remove interface, use `*daemon.MediaHandler` directly in `aiBatchHandler`. | ~5 |
| 8 | `cmd/jellywatch/orphans.go:18-22` | `orphansClient` interface — one method, one implementation (`jellyfin.Client`) | Interface with one implementation. Used only for test injection via package-level vars. | Pass `*jellyfin.Client` directly. For testing, use a test helper that constructs the real client with a mock HTTP transport. | ~5 |
| 9 | `cmd/jellywatch/scan_cmd.go:280` | `shouldRunPostScanAnalysis` — one-line function returning `showStats && analyze` | Unnecessary abstraction. Called exactly once. | Inline the boolean expression at the call site. | ~3 |
| 10 | `cmd/jellywatch/consolidate_generate.go:140` | `shouldReportSkippedConsolidationPlan` — one-line function | Unnecessary abstraction. Called exactly once. | Inline at call site. | ~3 |
| 11 | `cmd/jellywatch/config.go:330` | `separator(n int)` — one-line function returning `strings.Repeat("=", n)` | Unnecessary abstraction. Called once. | Inline `strings.Repeat("=", 40)` at call site. | ~3 |
| 12 | `cmd/jellywatchd/streaming_ops.go:340-350` | `notImplemented` function + `var _ = errors.New` | Dead code. `notImplemented` is never registered. `errors.New` import is unused (only the blank import remains). | Delete both. | ~10 |
| 13 | `cmd/classification-audit/main.go:1-219` | Standalone binary for one-off classification testing | Reads a hardcoded CSV path (`/tmp/media_files_test.csv`), has hardcoded year patterns (2022-2025) that will break. YAGNI as a binary. | Delete the binary. If the test logic is valuable, move to `internal/naming/` as a `_test.go` benchmark. | ~219 |
| 14 | `cmd/test-hint/main.go:1-44` | Standalone binary for one-off source hint testing | Hardcoded test path. YAGNI as a binary. | Delete. Move test to `internal/naming/` as a `_test.go`. | ~44 |
| 15 | `cmd/jellywatch/audit_validation.go:20-50` | `inferTypeFromLibraryRoot` — keyword matching on path strings with 10+ TV/movie keywords | Over-engineered. A library root is either under `cfg.Libraries.TV` or `cfg.Libraries.Movies` — the config already knows the type. | Replace with `isTVLibrary(path, cfg)` that checks membership in `cfg.Libraries.TV`. | ~20 |
| 16 | `cmd/jellywatch/audit_validation.go:60-120` | `validateMediaType` — makes live Sonarr/Radarr API calls during audit generation | Expensive and fragile. Network calls in a CLI generation loop. API errors are silently ignored. | Remove API validation. Library root path check is sufficient. If cross-validation is needed, do it post-generation as a separate step. | ~60 |
| 17 | `cmd/jellywatchd/control.go:200-215` | `guardMutator` — thin wrapper that could be inlined | Wraps a streaming handler with a pending-ops check. Used at registration time. | Inline the check in each handler registration, or make it a method on the server. | ~10 |
| 18 | `cmd/jellywatchd/jobs_handlers.go:430-445` | `payloadIntPtrLocal` — mirrors `housekeeping.payloadIntPtr` | Duplicated type-coercion logic. | Import and use `housekeeping.payloadIntPtr` directly (export it). | ~15 |
| 19 | `cmd/jellywatch/consolidate_cmd.go:200-230` | `updateDatabaseAfterMove` + `createMediaFileEntry` — DB update logic in CLI layer | Database mutation logic belongs in the database package, not the CLI command file. | Move to `internal/database/` as `UpdatePathAfterMove(source, target, size)`. | ~30 |
| 20 | `cmd/jellywatch/daemon_cmd.go:40-60` | `formatDaemonIPCError` — complex error classification via string matching on error messages | Fragile. Relies on substring matching of error text. | Use `errors.Is` / `errors.As` with typed sentinel errors from the IPC package. | ~15 |

## Needs Deeper Research

| # | Finding | Why cross-package |
|---|---------|-------------------|
| C1 | **`isMediaFile`/`isVideoFile` duplication** spans `cmd/jellywatch/main.go`, `cmd/jellywatchd/main.go`, `cmd/jellywatch/cleanup.go`, and possibly `internal/` packages. | Need to check if `internal/organizer`, `internal/scanner`, `internal/watcher` also have their own copies. A single `internal/mediautil.IsMediaFile(path)` would eliminate 3-5 copies. |
| C2 | **ProgressBar proliferation** — `ScanProgressBar` in scan_progress.go, `ProgressBar` in audit_validation.go, and inline progress in audit_cmd.go. Also check `internal/scanner` for yet another copy. | Consolidate into one `internal/progress` package. All 3 CLI files + any internal copies would use it. |
| C3 | **Plan file management** — `plans.SaveAuditPlans`, `plans.LoadAuditPlans`, `plans.DeleteAuditPlans`, `plans.SaveConsolidatePlans`, `plans.LoadConsolidatePlans`, `plans.DeleteConsolidatePlans`, `plans.ArchiveConsolidatePlans`, `plans.SaveDuplicatePlans`, `plans.LoadDuplicatePlans`, `plans.DeleteDuplicatePlans`, `plans.ArchiveDuplicatePlans`. | 11 plan-file functions for 3 plan types. Could be 3 generic functions (`SavePlan`, `LoadPlan`, `DeletePlan`) with a type parameter. Check `internal/plans/` for refactor opportunity. |
| C4 | **`newConsolidateCmd` / `newDuplicatesCmd` structural duplication** — both have generate/execute/dry-run subcommands with identical patterns. | Could be a generic `newPlanCmd(name, generateFn, executeFn, dryRunFn)`. Check if other plan-type commands (audit?) follow the same pattern. |
| C5 | **`audit_preview.go` charmbracelet dependency** — is `bubbles`/`tea`/`lipgloss` used elsewhere in the project? | If audit_preview.go is the only consumer, dropping it removes 3 dependencies from go.mod. Check `cmd/jellywatch/scan_progress.go` (also uses lipgloss) and `go.mod`. |
| C6 | **`classification-audit` and `test-hint` binaries** — are these referenced by any Makefile, CI script, or documentation? | Check `Makefile`, `.github/workflows/`, and `README.md` for references before deleting. |
