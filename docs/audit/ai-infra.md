# AI & Infra Ponytail Audit

## Findings

| Rank | File:Line | What | Why | Fix | Lines Saved |
|------|-----------|------|-----|-----|-------------|
| 1 | `internal/ai/recovery.go:51`, `internal/library/selector.go:445` | Custom `min()` functions | Go 1.21+ has built-in `min`/`max`. Project uses Go 1.26. | Delete both, use built-in `min()`. | 8 |
| 2 | `internal/logging/rotate.go:1-198` | Full custom log rotation (198 lines) | Reinvents `lumberjack` — gzip, backup numbering, age pruning. Stdlib has no rotation, but this is a solved problem. | Replace with `gopkg.in/natefinch/lumberjack.v2` (1 dep, ~3 lines config). Or keep if zero-dependency is a hard requirement. | ~190 |
| 3 | `internal/ai/cache.go:170` | `NormalizeInput` is a no-op | `func NormalizeInput(input string) string { return input }` — comment says "could add normalization in future". Scaffolding. | Delete function, inline the identity. | 3 |
| 4 | `internal/analyzer/analyzer.go:199-215` | `GetCleanupFiles` and `GetCleanupFilesPreserveExtras` are identical | Both methods have the exact same body. One is dead code or copy-paste artifact. | Delete `GetCleanupFilesPreserveExtras`, rename callers to `GetCleanupFiles`. | 7 |
| 5 | `internal/llm/interface.go:1-91`, `internal/mediamanager/interface.go:1-81` | Duplicated registry pattern | Both implement identical `map[string]T` registry with `Register`, `Get`, `All`, `AllInfo`. Same abstraction, different types. | Extract one generic registry or just use a `map` directly at call sites. | ~50 |
| 6 | `internal/llm/types.go:7-13` | 4 speculative provider type constants | `ProviderTypeOpenAI`, `ProviderTypeAnthropic`, `ProviderTypeLMStudio`, `ProviderTypeCustom` — only Ollama is implemented. | Delete unused constants. Add when implementations exist. | 4 |
| 7 | `internal/ai/status.go:1-186` | `AIStatus` + `AIStatusSnapshot` are field-identical structs | Mutable status and its immutable snapshot copy have the same 20 fields. The snapshot exists only to return a copy under RLock. | Return a value copy of `AIStatus` directly (or a subset). Delete `AIStatusSnapshot`. | ~30 |
| 8 | `internal/ai/queue.go:247-253` | `QueueError` custom error type | `struct{ msg string }` with `Error()` — identical to `errors.New`/`fmt.Errorf`. | Replace with `errors.New` sentinel errors. | 7 |
| 9 | `internal/ai/queue.go:48-49` | Unused `outputChan` | `Complete()` and `Fail()` write to `outputChan`, `Output()` exposes it, but nothing reads from it. Dead channel. | Delete `outputChan`, `Output()`, and the `select` writes in `Complete`/`Fail`. | ~10 |
| 10 | `internal/ai/integrator.go:52-54` | Dual shutdown: `shutdownCh` + `bgCtx`/`bgCancel` | `backgroundWorker` listens to both channels for the same purpose. `Close()` closes both. | Use only `bgCtx`/`bgCancel`. Delete `shutdownCh`. | ~5 |
| 11 | `internal/ai/keepalive.go:88-97`, `internal/ai/cache.go:148` | `fmt.Printf` instead of structured logger | Keepalive and cache use `fmt.Printf("[Keepalive] ...")` and `fmt.Printf("[WARN] ...")` — bypasses the project's structured logger. | Use the `logging.Logger` instance. | 0 (quality) |
| 12 | `internal/mediamanager/radarr_adapter.go:183-189` | `GetQualityProfiles` and `GetHistory` return `nil, nil` | Dead interface methods. Stub implementations that do nothing. | Return `nil, fmt.Errorf("not implemented")` or remove from interface if unused. | 0 (honesty) |
| 13 | `internal/mediamanager/sonarr_adapter.go:183-185` | `GetQualityProfiles` returns `nil, nil` | Same dead stub. | Same fix. | 0 |
| 14 | `internal/wizard/consolidate.go:56` | `// TODO: Implement actual move` | Dead code path — consolidation execution is a TODO. User-facing feature that silently does nothing. | Either implement or remove the "consolidate" step from wizard until ready. | 0 (honesty) |
| 15 | `internal/wizard/duplicates.go:82` | `// Swap not implemented yet` | Dead feature in interactive wizard. | Remove the swap option from the prompt until implemented. | 2 |
| 16 | `internal/postmortem/suspicious.go:73-97` | Hardcoded storage path translation map | 24 hardcoded path mappings (`/mnt/STORAGE1/` → `/storage1/`, etc.). Environment-specific config smuggled into code. | Move to config file or derive from configured library paths. | ~25 |
| 17 | `internal/postmortem/collector.go:131-152` | `parseDecisionEvidence` mirrors `database.ParseDecision` field-for-field | Copy-paste of DB struct for JSON serialization. 22 fields duplicated. | Add `json` tags to `database.ParseDecision` directly, or use a simple mapper. | ~20 |
| 18 | `internal/postmortem/collector.go:247-265` | `mediaInventory` loads ALL media files to count quality buckets | Calls `GetAllMediaFiles()`, iterates every row to bin by `QualityScore`. | Use a SQL `GROUP BY` query: `SELECT CASE WHEN quality_score >= 10... COUNT(*) FROM media_files GROUP BY 1`. | ~15 (and O(1) memory) |
| 19 | `internal/postmortem/collector.go:268-282` | `configSnapshot` hand-picks 10 config fields | Fragile — any new config field is silently omitted from postmortem reports. | Serialize the whole config struct (with sensitive fields redacted via json tag). | ~10 |
| 20 | `internal/library/selector.go:370-420` | `findRelatedContent` and `countMediaItems` walk entire library filesystem | O(n) filesystem walk on every library selection. DB already has this data. | Query `media_files` table instead. | ~50 (and O(1) from O(n)) |
| 21 | `internal/library/selector.go:27` | `CacheDuration` field marked "deprecated - use DB" | Dead config field still in struct. | Remove field. | 2 |
| 22 | `internal/library/scanner_helper.go:1-15` | Single 15-line function in its own file | `extractYearFromDir` is one regex function. | Inline into `scanner.go`. Delete file. | 15 (file) |
| 23 | `internal/service/analysis.go:72,87,127` | Double-pruning of missing files | `AnalyzeDuplicates` calls `PruneMissingMediaFiles()` (scans ALL files), then `pruneMissingMediaFiles()` per group (scans again). Same os.Stat+delete logic duplicated. | Call `PruneMissingMediaFiles()` once at start, remove per-group `pruneMissingMediaFiles`. | ~30 |
| 24 | `internal/service/analysis.go:287-293` | `isScatteredMediaFile` duplicates `isVideoFile` | Same extension list as `analyzer/analyzer.go:230-235` (`videoExtensions`). | Use `analyzer.isVideoFile` (export it) or share the extension set. | 7 |
| 25 | `internal/wizard/wizard.go:103-112`, `internal/llm/ollama_adapter.go:203-210` | Duplicated `formatBytes` | Two identical byte-formatting functions. | Extract to `internal/format` or use a single shared util. | 10 |
| 26 | `internal/analyzer/analyzer.go:230-235`, `internal/library/scanner.go:108-112` | Duplicated video extension maps | `videoExtensions` map and `videoExts` map are near-identical. | Share one definition. | 8 |
| 27 | `internal/ai/integrator.go:258-290` | `updateImprovementStatus` and `saveImprovementResult` are two separate DB writes | Both write to `ai_improvements` table. Could be one method. | Merge into single upsert. | ~15 |
| 28 | `internal/ai/types.go:1-142` | `FlexInt` and `FlexIntSlice` custom JSON types | Handle Ollama's inconsistent JSON (int/string/null). Legitimate but complex for what it does. | Keep — this is a real trust-boundary need. But `FlexIntSlice` string parsing (`extractEpisodeNumber`) could be simpler. | 0 (keep) |
| 29 | `internal/ai/matcher.go:1-440` | `ParseWithRetry`, `ParseWithCloud`, `ParseWithContext` — 4 parse variants | `Parse`, `ParseWithContext`, `ParseWithRetry`, `ParseWithCloud` all call `parseWithModel`. Context/retry/cloud are composition concerns, not separate methods. | Keep `Parse` + `ParseWithContext`. Fold retry into `parseWithModel`. Cloud model is just a config switch. | ~30 |
| 30 | `internal/ai/matcher.go:340-440` | `getSystemPrompt` is a 100-line string literal | Giant prompt embedded in Go source. Hard to iterate on, bloats the file. | Move to embedded file (`//go:embed prompt.txt`) or config. | ~100 (moved, not deleted) |
| 31 | `internal/ai/integrator.go:1-430` | `Integrator` orchestrates 6 subsystems | Matcher, Cache, CircuitBreaker, Keepalive, BackgroundQueue, AIStatus — all wired in one constructor. High coupling. | Accept if this is the integration point. But `DatabaseProvider` interface with one method (`DB() *sql.DB`) is an abstraction for one implementation. | 0 (structural) |
| 32 | `internal/ai/circuit_breaker.go:1-137` | Custom circuit breaker | Standard pattern, well-implemented. No stdlib equivalent. | Keep. | 0 |
| 33 | `internal/notify/notify.go:1-210` | `Manager` async mode with unused result channel | `Results()` exposes channel but no caller reads it. Async mode spawns goroutines that write to a channel nobody drains. | Remove async mode or add a consumer. | ~20 |
| 34 | `internal/notify/jellyfin.go:1-187` | `targetedRefresh` falls through to `refreshLibrary` | If targeted refresh fails, it does a full library refresh. This is a deliberate fallback but doubles the API calls on every failure. | Keep — this is correct behavior for reliability. | 0 |
| 35 | `internal/library/cache.go:1-111` | `SeriesCache` custom TTL cache | Custom refresh-on-expiry cache with mutex, `refreshing` flag, background refresh. | Keep — Sonarr API is slow, caching is needed. But `ForceRefresh` is unused (check callers). | 0 (check ForceRefresh) |
| 36 | `internal/service/convergence.go:1-160` | `GroupConfidence` complex heuristic | 160 lines of confidence classification with nested size/quality/resolution logic. Legitimate business logic for safe auto-deletion. | Keep — this is the core safety gate. | 0 |
| 37 | `internal/validator/validator.go:1-176` | `Validator` with functional options for one boolean | `WithAllowMissingYear` functional option pattern for a single `allowMissingYear` bool. Over-engineered for one flag. | Pass `allowMissingYear` as a constructor param directly. | ~10 |
| 38 | `internal/permissions/permissions.go:1-145` | `PermissionError` custom error with sudo instructions | Custom error type that formats chown/chmod commands. Nice UX but 145 lines for permission checking. | Keep — trust boundary. But `CanDelete` checks both dir writability AND file writability; on Linux only dir write matters for deletion. | ~5 |
| 39 | `internal/privilege/escalate.go:1-36` | `Escalate` re-execs with sudo | 36 lines, clean. Uses `syscall.Exec`. | Keep. | 0 |
| 40 | `internal/tmdb/verifier.go:1-390` | TMDB verifier with 3-tier lookup | Local DB → cache → TMDB API → Jellyfin fallback. Well-structured, legitimate complexity. | Keep. | 0 |
| 41 | `internal/postmortem/collector.go:1-387` | Collector writes 11 files per run | Each `writeJSON`/`writeText` call is a separate file. Legitimate for postmortem bundles. | Keep. | 0 |
| 42 | `internal/service/rebuild_movies.go:1-117` | `RebuildMoviesFromMediaFiles` rebuilds movie table from media_files | Legitimate data migration/repair operation. | Keep. | 0 |

## Needs Deeper Research

| # | Finding | Packages Involved | Why Cross-Package |
|---|---------|-------------------|-------------------|
| CR1 | `normalizeTitle` in `library/scanner.go:127` vs `database.NormalizeForMatch` | `library`, `database` | Two title normalization functions may diverge. Check if they produce identical results for all inputs. If so, delete the library one. |
| CR2 | `SonarrAdapter` (234 lines) vs `RadarrAdapter` (210 lines) — ~90% structural copy | `mediamanager`, `sonarr`, `radarr` | Both adapters have identical method shapes. Could a single generic adapter with a type parameter on the underlying client work? The `*arr` clients have similar APIs. |
| CR3 | `videoExtensions` map exists in 3+ places | `analyzer`, `library`, `service` | `analyzer/analyzer.go:230`, `library/scanner.go:108`, `service/analysis.go:287` all define video extension lists. Consolidate to one canonical source. |
| CR4 | `formatBytes` exists in 2 places, `min` in 2 places | `wizard`, `llm`, `ai`, `library` | Shared utility functions scattered across packages. Create `internal/util` or use stdlib (`min` is built-in). |
| CR5 | `DatabaseProvider` interface in `ai/integrator.go:56` — one method, one implementation | `ai`, `service` | `type DatabaseProvider interface { DB() *sql.DB }` — is this used polymorphically anywhere? If not, pass `*sql.DB` directly. |
| CR6 | `SeriesCache.ForceRefresh` — is it called anywhere? | `library` | Check all callers. If unused, delete. |
| CR7 | `BackgroundQueue.Output()` — is the output channel consumed anywhere? | `ai` | Check all callers. If unused, delete `outputChan` and related code. |
| CR8 | `NotifyManager.Results()` — is the async result channel consumed? | `notify` | Check all callers. If unused, delete async mode. |
| CR9 | `LLMProvider` interface — is `ProviderRegistry` used anywhere outside tests? | `llm` | If the registry and interface exist only for future multi-provider support, they're YAGNI. Check call sites. |
| CR10 | `MediaManager` interface — is `Registry` used polymorphically? | `mediamanager` | Two implementations exist (Sonarr/Radarr), which justifies the interface. But check if the registry abstraction is actually used vs direct adapter construction. |
