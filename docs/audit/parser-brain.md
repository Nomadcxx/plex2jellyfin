# Parser Brain Ponytail Audit

## Findings

| Rank | File:Line | What | Why | Fix | Lines Saved |
|------|-----------|------|-----|-----|-------------|
| 1 | quality/scoring.go:141-170 | `contains`, `stringContains`, `indexOfSubstring`, `toLower` — hand-rolled reimplementations of `strings.Contains` and `strings.ToLower` | Rung 2: stdlib has `strings.Contains` and `strings.ToLower`. These 4 functions (30 lines) are pure reinvention. | Delete all 4 functions. Replace `contains(s, substr)` with `strings.Contains(strings.ToUpper(s), substr)`. | ~30 |
| 2 | naming/advanced.go:1-200 | `init()` pre-compiles ~50 regexes into `advReleasePatterns` — 80% duplicates of `releasePatterns` in `naming.go:init()` | Rung 1: two `init()` blocks in the same package compiling nearly identical regex lists. The `advReleasePatterns` list adds ~15 extra patterns (HDR variants, streaming platforms, commentary markers, bracketed content, bit depth) but the core 35 patterns are duplicates. | Merge `advReleasePatterns` into `releasePatterns` (or vice versa). Keep the union of patterns in ONE list. Delete the duplicate `init()`. | ~150 |
| 3 | naming/blacklist.go:1-105000 | `KnownReleaseGroups` — 10,992-entry map built from srrDB.com dump. 105KB of string data. | Rung 1: `naming.go` already has a `knownReleaseGroups` map with ~80 entries. Does the 10k list measurably improve parse accuracy over the 80-entry list? The 80-entry list covers the most common groups (RARBG, YTS, YIFY, SPARKS, FGT, etc.). The 10k list includes thousands of obscure/defunct groups that will never appear in real filenames. | Measure accuracy with 80-entry list vs 10k list. If 80-entry covers 95%+ of cases, delete this file entirely. If needed, keep only groups that appear in actual user filenames (log-driven). | ~105000 |
| 4 | naming/advanced.go:202-350 | `StripReleaseGroup` — 150-line function that does what `stripReleaseMarkers` (40 lines in naming.go) already does, plus abbreviation preservation with § placeholders | Rung 1: two release-stripping functions in the same package. `stripReleaseMarkers` handles the common case. `StripReleaseGroup` adds: pre-hyphen regex stripping, abbreviation preservation/restoration, hyphen-aware tokenization, orphan hyphen-suffix removal. The abbreviation preservation is clever but the core stripping logic is duplicated. | Merge into one function. Keep the abbreviation preservation if it measurably fixes real-world cases; otherwise drop it. | ~110 |
| 5 | naming/advanced.go:352-380 | `ExtractYearAdvanced` — duplicates `extractYear` from naming.go with extra resolution-value filtering (2160, 1920, 1440, 1280) | Rung 1: `extractYear` already filters 2160. The extra filtering of 1920/1440/1280 is marginal. Two year-extraction functions in one package. | Delete `ExtractYearAdvanced`. Add the resolution-value skip list to `extractYear`. | ~25 |
| 6 | naming/advanced.go:382-390 | `removeYearAdvanced` — duplicates `removeYear` from naming.go | Rung 1: identical purpose, different implementation (regex-based vs string replace). | Delete. Use `removeYear`. | ~8 |
| 7 | naming/advanced.go:392-410 | `removeSpecificYear` — compiles regex per call with `regexp.MustCompile` | Rung 2: `removeYear` in naming.go uses `strings.ReplaceAll` which is faster and doesn't compile per call. Same purpose. | Delete. Use `removeYear`. | ~18 |
| 8 | naming/advanced.go:412-420 | `isRomanNumeralTitleToken` — 10-line switch for roman numerals II-X | Rung 5: could be `map[string]bool` lookup. Fine as-is but switch is verbose. | `var romanTokens = map[string]bool{"II":true,...}` then `romanTokens[strings.ToUpper(strings.TrimSpace(word))]` | ~5 |
| 9 | naming/advanced.go:422-520 | `stripOrphanedReleaseGroups` — 100 lines of heuristic tail-stripping with multiple condition layers (roman numeral check, acronym check, common word check, known media title check, known release group check, all-caps check, title-case check, digit+letter check, language tag check) | Rung 1: this is a "last resort" cleanup that tries to remove leftover group names after regex stripping fails. The heuristic layers are fragile — each new layer was added to fix a false positive from the previous layer. The function has 8 separate `break`/`continue` conditions. | Simplify to: strip tail words that are in `KnownReleaseGroups` AND are not in `knownMediaTitles` AND are not common English words. Drop the all-caps/title-case/digit-letter heuristics. | ~60 |
| 10 | naming/advanced.go:522-620 | `IsGarbageTitle` — 100 lines with garbage ratio calculation, leetspeak detection, all-caps detection, stopword filtering | Rung 1: complex heuristic that tries to determine if a parsed title is garbage. The 50% ratio threshold is arbitrary. The leetspeak detection (`hasLeetPattern`) is a nested loop. | Simplify to: if all words are in `KnownReleaseGroups` or are codec markers, it's garbage. Drop the ratio calculation and leetspeak detection. | ~70 |
| 11 | naming/advanced.go:622-700 | `CleanMovieName` — 80-line "advanced" movie name cleaner that chains: extension strip → year extract → pre-year truncation → `StripReleaseGroup` → `removeSpecificYear` → `stripOrphanedReleaseGroups` → `titleCaseWithOrdinals` → double-dot cleanup → year append | Rung 1: `parseMovieSimple` + `parseMovieAdvanced` in naming.go already handle movie name parsing. `CleanMovieName` is a third path that duplicates the pipeline. The pre-year truncation logic (stripping everything after the year) is the only unique part. | Move the pre-year truncation into `parseMovieSimple` or `parseMovieAdvanced`. Delete `CleanMovieName`. | ~70 |
| 12 | naming/advanced.go:702-780 | `titleCaseWithOrdinals` — 80 lines with placeholder-based preservation of ordinals, abbreviations (U.S., R.I.P.D.), and acronyms (8MM, RIPD). Uses `golang.org/x/text/cases` external dependency. | Rung 4: `golang.org/x/text` is already installed (used for `cases.Title`). But the placeholder dance (§§§0§§§, §§§1§§§, §§§2§§§) with three separate maps and regex re-compilation per call is over-engineered. | Use `cases.Title` directly. Handle ordinals with a simple post-processing regex: `regexp.MustCompile(`(?i)\b(\d+)(st|nd|rd|th)\b`).ReplaceAllStringFunc`. Drop abbreviation/acronym preservation unless proven necessary. | ~50 |
| 13 | naming/advanced.go:782-830 | `NormalizeName` — 50 lines for fuzzy-match normalization. Chains `StripReleaseGroup` → `removeYearAdvanced` → lowercase → roman numeral substitution → word substitution → punctuation removal → space collapse. | Rung 1: the roman numeral map and word substitution map are fine. But it calls `StripReleaseGroup` (150 lines) and `removeYearAdvanced` (duplicate). | Use `stripReleaseMarkers` + `removeYear` instead. | ~5 |
| 14 | naming/advanced.go:832-850 | `ExtractResolution` — simple `strings.Contains` chain | Rung 5: fine as-is. Could be a map lookup but current form is readable. | Keep. | 0 |
| 15 | naming/advanced.go:852-900 | `SimilarityRatio` + `levenshteinDistance` + `minThree` — 50 lines of custom Levenshtein implementation | Rung 2: Go has no stdlib Levenshtein. But `minThree` could be `min(min(a,b),c)` using Go 1.21+ builtin `min`. The full matrix allocation is O(n*m) memory. | Replace `minThree` with `min(min(a,b),c)`. Consider if similarity is even used — if only for fuzzy dedup, a simpler Jaccard or token-overlap might suffice. | ~5 |
| 16 | naming/blacklist.go:1-30 | `buildReleaseGroupMap()` — builds map from slice literal | Rung 5: could just be a map literal. The function adds an unnecessary indirection. | `var KnownReleaseGroups = map[string]bool{...}` directly. | ~3 |
| 17 | naming/confidence.go:1-10 | `codecSuffixRegex`, `resolutionSuffixRegex`, `ordinalTokenRegex` — pre-compiled regexes | Rung 5: fine, pre-compilation is correct. | Keep. | 0 |
| 18 | naming/confidence.go:12-50 | `CalculateTitleConfidence` — 40-line scoring function with 7 penalty/bonus factors | Rung 1: the confidence score is used to decide whether to trust a parse. But the penalties are arbitrary (-0.8, -0.9, -0.5, -0.4, -0.1) with no empirical calibration. The `isTVPattern` floor of 0.5 is a band-aid. | Simplify to binary: is the parse trustworthy or not? Drop the float score. | ~30 |
| 19 | naming/confidence.go:52-70 | `shouldApplyGarbageTitlePenalty` — 20 lines deciding when to apply garbage penalty | Rung 1: exists solely to prevent false positives from the over-broad `IsGarbageTitle`. If `IsGarbageTitle` is simplified (finding #10), this function becomes unnecessary. | Delete when `IsGarbageTitle` is simplified. | ~18 |
| 20 | naming/confidence.go:72-85 | `isStandaloneReleaseArtifact` — 15 lines | Rung 5: fine, simple check. | Keep. | 0 |
| 21 | naming/confidence.go:87-110 | `isTechnicalTitleToken` — 25 lines with codec check, source check, digit+letter heuristic | Rung 1: the digit+letter heuristic at the end is a catch-all that could false-positive on legitimate titles like "Se7en" or "8MM". | Drop the digit+letter heuristic. Keep only the explicit codec/source checks. | ~8 |
| 22 | naming/confidence.go:112-116 | `isTVPattern` + `tvPatternForConfidence` — 3 lines wrapping a regex | Rung 5: could inline the regex. But pre-compiled is fine. | Keep. | 0 |
| 23 | naming/confidence.go:118-170 | `hasDuplicateYear` — 50 lines with position tracking, paren detection, and metadata collection | Rung 1: the goal is to detect "Matrix (2001) (2001)". A simpler approach: count year occurrences, if >1 and not all at position 0, it's a duplicate. The position-tracking with `isWithinParen` is over-engineered. | `len(regexp.MustCompile(`\b(19|20)\d{2}\b`).FindAllString(s, -1)) > 1` with a check that the first occurrence isn't at position 0 (title year). | ~35 |
| 24 | naming/confidence.go:172-200 | `endsWithCodecOrSource`, `hasResolutionInTitle`, `hasReleaseMarkers` — 30 lines of simple checks | Rung 5: fine. `hasReleaseMarkers` hardcodes a list that overlaps with `knownReleaseGroups`. | Keep, but use `knownReleaseGroups` map for the marker list instead of hardcoding. | ~5 |
| 25 | naming/confidence.go:202-210 | `clamp` — 8 lines | Rung 2: `math.Max(math.Min(value, max), min)` does this in one line. | Delete. Use stdlib. | ~7 |
| 26 | naming/deobfuscate.go:1-15 | `hexStringRegex`, `uuidRegex`, `base64LikeRegex` — pre-compiled | Rung 5: fine. | Keep. | 0 |
| 27 | naming/deobfuscate.go:17-60 | `IsObfuscatedFilename` — 45 lines with 7 checks chained | Rung 1: the chain of checks is reasonable but `IsGarbageTitle` + `hasHighEntropy` combo check is fragile. The `isRandomAlphaToken` check (16+ chars, no digits, high entropy, low vowels) is very specific. | Keep structure but drop the `IsGarbageTitle` + `hasHighEntropy` combo (use simpler entropy check). | ~10 |
| 28 | naming/deobfuscate.go:62-75 | `isShortRandomAlphanumeric` — 15 lines | Rung 5: fine, specific heuristic. | Keep. | 0 |
| 29 | naming/deobfuscate.go:77-85 | `isRandomAlphaToken` — 10 lines | Rung 1: very narrow heuristic (16+ alpha-only chars, high entropy, <15% vowels). Only catches a specific obfuscation pattern. | Keep if it catches real cases; otherwise drop. | ~8 |
| 30 | naming/deobfuscate.go:87-130 | `isRandomAlphanumeric` — 45 lines with separator check, case/digit ratio analysis, entropy check | Rung 1: the digit ratio bounds (0.05-0.5) and entropy threshold (>0.4) are arbitrary. The separator check (rejecting any string with `.`, `-`, `_`, ` `) is good. | Simplify: if 20+ chars, pure alphanumeric, has mixed case + digits, and high entropy → obfuscated. Drop the digit ratio bounds. | ~15 |
| 31 | naming/deobfuscate.go:132-170 | `isPlainAlphanumeric`, `characterClasses`, `vowelRatio`, `hasHighEntropy` — 40 lines of utility functions | Rung 5: these are used by the obfuscation detectors. `characterClasses` returns 3 values from one loop — efficient. `vowelRatio` and `hasHighEntropy` are simple. | Keep. | 0 |
| 32 | naming/deobfuscate.go:172-180 | `ParseTVShowFromPath` — wrapper that calls `ParseTVShowFromPathVerbose` and discards tokens | Rung 1: convenience wrapper. Callers can just use `ParseTVShowFromPathVerbose` and ignore the second return value. | Delete. | ~4 |
| 33 | naming/deobfuscate.go:182-210 | `ParseTVShowFromPathVerbose` — 30 lines walking up 3 parent dirs | Rung 5: fine, necessary for obfuscated files. | Keep. | 0 |
| 34 | naming/deobfuscate.go:212-220 | `ParseMovieFromPath` — wrapper that calls `ParseMovieFromPathVerbose` and discards tokens | Rung 1: same as #32. | Delete. | ~4 |
| 35 | naming/deobfuscate.go:222-260 | `ParseMovieFromPathVerbose` — 40 lines walking up 3 parent dirs with `isUsableMovieFolderCandidate` check | Rung 5: fine. | Keep. | 0 |
| 36 | naming/deobfuscate.go:262-275 | `isUsableMovieFolderCandidate` — 15 lines | Rung 5: fine. | Keep. | 0 |
| 37 | naming/deobfuscate.go:277-290 | `isGenericMovieContainerFolder` — 15 lines | Rung 5: could be `map[string]bool` lookup. | `var genericFolders = map[string]bool{"movie":true, "movies":true, "film":true, "films":true, "cinema":true}` | ~8 |
| 38 | naming/deobfuscate.go:292-330 | `IsTVEpisodeFromPath` — 40 lines with SourceHint logic and parent-dir walking | Rung 5: fine, necessary. | Keep. | 0 |
| 39 | naming/deobfuscate.go:332-336 | `IsMovieFromPath` — negates `IsTVEpisodeFromPath` | Rung 1: one-line wrapper. Callers can use `!IsTVEpisodeFromPath(...)`. | Delete. | ~3 |
| 40 | naming/deobfuscate.go:338-350 | `ParseError` struct + `Error()` + `Unwrap()` — 15 lines | Rung 5: custom error type with `Unwrap` to `ErrParseFailed`. Fine. | Keep. | 0 |
| 41 | naming/jellyfin_naming.go:1-15 | `jellyfinEpisodePattern`, `jellyfinMoviePattern`, `releaseMarkerPattern`, `jellyfinReleaseGroupSuffix` — pre-compiled regexes | Rung 5: fine. | Keep. | 0 |
| 42 | naming/jellyfin_naming.go:17-50 | `IsJellyfinCompliantFilename` — 35 lines | Rung 5: fine, specific check. | Keep. | 0 |
| 43 | naming/jellyfin_naming.go:52-60 | `getExtension` — 10 lines, manual reverse-scan for file extension | Rung 2: `filepath.Ext(filename)` returns the extension with dot. `strings.TrimPrefix(ext, ".")` gives the same result. | `strings.TrimPrefix(filepath.Ext(filename), ".")` | ~8 |
| 44 | naming/jellyfin_naming.go:62-95 | `hasReleaseGroupSuffix` — 35 lines with uppercase detection and `IsKnownReleaseGroup` lookup | Rung 5: fine. | Keep. | 0 |
| 45 | naming/known_titles.go:1-100 | `knownMediaTitles` — ~100-entry map | Rung 5: necessary to prevent false positives from the release group blacklist. | Keep. | 0 |
| 46 | naming/known_titles.go:102-108 | `IsKnownMediaTitle` — 5-line wrapper | Rung 5: fine. | Keep. | 0 |
| 47 | naming/naming.go:1-30 | `ErrParseFailed`, `MovieInfo`, `TVShowInfo`, regex vars | Rung 5: fine. | Keep. | 0 |
| 48 | naming/naming.go:32-80 | `init()` — compiles ~30 patterns into `releasePatterns` | Rung 1: 80% overlap with `advanced.go:init()`. See finding #2. | Merge with advanced.go patterns. | ~40 |
| 49 | naming/naming.go:82-130 | `IsMovieFilename`, `IsTVEpisodeFilename`, `HasYearInParentheses`, `ParseMovieName`, `ParseMovieNameVerbose`, `parseMovieFromBaseName`, `parseMovieSimple` — core parsing pipeline | Rung 5: fine. `parseMovieSimple` calls `stripReleaseMarkers` then falls back to `parseMovieAdvanced` (which calls `CleanMovieName`). | Keep structure but simplify fallback chain (see #11). | 0 |
| 50 | naming/naming.go:132-150 | `parseMovieAdvanced` — calls `CleanMovieName` from advanced.go | Rung 1: `CleanMovieName` is over-engineered (see #11). This fallback path exists because `parseMovieSimple` sometimes fails. | After simplifying `CleanMovieName`, this becomes a thin wrapper. | ~5 |
| 51 | naming/naming.go:152-200 | `ParseTVShowName`, `ParseTVShowNameVerbose`, `parseTVShowFromBaseName` — TV parsing pipeline | Rung 5: fine. | Keep. | 0 |
| 52 | naming/naming.go:202-250 | `IsTVSeasonPackName`, `ParseTVSeasonPackName`, `ParseTVSeasonPackNameVerbose` — season pack parsing | Rung 5: fine. | Keep. | 0 |
| 53 | naming/naming.go:252-265 | `NormalizeMovieName`, `NormalizeTVShowName` — identical functions | Rung 1: two functions with identical bodies. | Merge into one: `NormalizeTitle(title, year string) string`. | ~8 |
| 54 | naming/naming.go:267-295 | `FormatMovieFilename`, `FormatTVEpisodeFilename`, `FormatSeasonFolder`, `FormatTVEpisodeFilenameFromInfo` — formatting functions | Rung 5: fine. `FormatMovieFilename` and `FormatTVEpisodeFilename` share the same year-in-parens pattern but differ in suffix. Could be one function with a suffix param, but current form is clear. | Keep. | 0 |
| 55 | naming/naming.go:297-310 | `knownReleaseGroups` — local ~80-entry map | Rung 1: duplicates concept from `blacklist.go:KnownReleaseGroups` (10,992 entries). Two different maps with the same purpose in the same package. | Consolidate. If blacklist.go is kept, use `KnownReleaseGroups` everywhere. If deleted, keep this 80-entry map. | ~5 |
| 56 | naming/naming.go:312-315 | `qualityMarkerDetect`, `releaseGroupSuffix` — pre-compiled regexes | Rung 5: fine. | Keep. | 0 |
| 57 | naming/naming.go:317-360 | `collectStrippedTokens` — 45 lines collecting tokens that `stripReleaseMarkers` would remove | Rung 1: this function exists to return "what was stripped" for verbose parsing. It duplicates the stripping logic of `stripReleaseMarkers` (phases 1 and 2 are identical). | Refactor `stripReleaseMarkers` to also return stripped tokens, or have `collectStrippedTokens` call a shared helper. | ~20 |
| 58 | naming/naming.go:362-400 | `stripReleaseMarkers` — 40 lines, core release stripping | Rung 5: fine. Phases 1 and 2 are duplicated in `collectStrippedTokens`. | See #57. | 0 |
| 59 | naming/naming.go:402-430 | `extractYear`, `removeYear` — 30 lines | Rung 5: fine. `removeYear` uses `strings.ReplaceAll` (good). | Keep. | 0 |
| 60 | naming/naming.go:432-500 | `findEpisodeMatch` — 70 lines with 4 regex patterns (SxxExx, Xxx, EPxx, date) | Rung 5: fine, core episode detection. | Keep. | 0 |
| 61 | naming/naming.go:502-520 | `extractEpisodeInfo`, `extractTitleBeforeEpisode` — thin wrappers | Rung 1: `extractEpisodeInfo` is unused if callers use `findEpisodeMatch` directly. `extractTitleBeforeEpisode` is a 5-line wrapper. | Check if `extractEpisodeInfo` has any callers. If not, delete. | ~8 |
| 62 | naming/naming.go:522-528 | `normalizeSpaces` — compiles `regexp.MustCompile(`\s+`)` per call | Rung 5: should be a package-level `var spacesRegex = regexp.MustCompile(`\s+`)` to avoid recompilation. | Move regex to package-level var. | ~2 |
| 63 | naming/types.go:1-25 | `SourceHint` enum + `String()` + `TVSeasonPackInfo` | Rung 5: fine. | Keep. | 0 |
| 64 | quality/extract.go:1-15 | `QualityMetadata` struct | Rung 5: fine. | Keep. | 0 |
| 65 | quality/extract.go:17-60 | `ExtractMetadata` — 45 lines, main entry point with parent-dir fallback | Rung 5: fine. The parent-dir walking (2 levels) is necessary for releases where quality markers are only in folder names. | Keep. | 0 |
| 66 | quality/extract.go:62-70 | `ExtractMovieMetadata`, `ExtractEpisodeMetadata` — 5-line convenience wrappers | Rung 1: callers can just call `ExtractMetadata(path, size, false)` directly. These wrappers save one boolean parameter. | Delete. | ~6 |
| 67 | quality/extract.go:72-90 | `CompareWithSize` — 20-line convenience wrapper | Rung 1: callers can call `ExtractMetadata` twice and compare scores. | Delete if only used in one place. | ~18 |
| 68 | quality/extract.go:92-115 | `FindBestFile` — 25 lines | Rung 5: fine, useful utility. | Keep. | 0 |
| 69 | quality/patterns.go:1-50 | ~30 pre-compiled regexes for resolution, source, HDR, audio, 3D, proper, extended | Rung 5: fine, pre-compilation is correct. | Keep. | 0 |
| 70 | quality/patterns.go:52-130 | `parseResolution`, `parseSource`, `parseHDR`, `parseAudio`, `parse3D`, `parseProper`, `parseExtended` — 80 lines of switch-based parsers | Rung 5: fine, straightforward. | Keep. | 0 |
| 71 | quality/patterns.go:132-145 | `CompareFiles`, `IsBetterFile`, `GetQualityString` — 15 lines of convenience wrappers | Rung 1: `CompareFiles` is used by `IsBetterFile`. `GetQualityString` is a one-liner. | Keep `CompareFiles` if used. Delete `GetQualityString` (inline the `.String()` call). | ~5 |
| 72 | quality/patterns.go:147-170 | `ParseFromPath` — 25 lines with parent-dir fallback for source detection | Rung 1: duplicates the parent-dir walking logic from `extract.go:ExtractMetadata` (lines 30-45). Two functions in the same package doing the same parent-dir fallback. | Delete `ParseFromPath`. Use `Parse` + manual parent-dir check, or consolidate into `ExtractMetadata`. | ~20 |
| 73 | quality/patterns.go:172-180 | `getExt` — 10 lines, manual reverse-scan for extension | Rung 2: duplicates `jellyfin_naming.go:getExtension` and `filepath.Ext` exists in stdlib. | Delete. Use `strings.TrimPrefix(filepath.Ext(path), ".")`. | ~8 |
| 74 | quality/quality.go:1-80 | Type definitions: `Source`, `Resolution`, `HDRFormat`, `AudioCodec` enums with iota constants | Rung 5: fine, necessary for type-safe quality comparison. | Keep. | 0 |
| 75 | quality/quality.go:82-100 | `QualityInfo` struct + `Parse` function | Rung 5: fine. | Keep. | 0 |
| 76 | quality/quality.go:102-180 | `ComputeScore` — 80-line switch-based scoring | Rung 5: fine. The scoring weights are arbitrary but the structure is clean. | Keep. | 0 |
| 77 | quality/quality.go:182-200 | `Compare`, `IsBetterThan` — 20 lines | Rung 5: fine. | Keep. | 0 |
| 78 | quality/quality.go:202-260 | `String` — 60-line human-readable string builder with 4 switch blocks | Rung 1: the `Source.String()` method (lines 262-285) duplicates the Source switch from `String()`. `ResolutionToString` and `SourceToString` in scoring.go also duplicate these switches. That's 4 copies of the same switch logic. | Keep ONE copy (e.g., the methods on the types). Delete `ResolutionToString`/`SourceToString`/`AudioToString` from scoring.go, use the type methods instead. | ~40 |
| 79 | quality/quality.go:262-285 | `Source.String()` — 25-line switch | Rung 1: duplicates the Source switch in `QualityInfo.String()`. See #78. | Keep this one, delete the duplicate in `String()` (call `q.Source.String()` instead). | ~15 |
| 80 | quality/scoring.go:1-30 | CONDOR scoring constants | Rung 5: fine, named constants are good. | Keep. | 0 |
| 81 | quality/scoring.go:32-90 | `ScoreFile` — 60-line scoring function with resolution + source + size | Rung 5: fine. The `UnknownResolutionMultiplier` (size * 50) is aggressive but intentional. | Keep. | 0 |
| 82 | quality/scoring.go:92-100 | `ScoreMovie`, `ScoreEpisode` — 5-line convenience wrappers | Rung 1: same as #66. Callers can pass the boolean directly. | Delete. | ~6 |
| 83 | quality/scoring.go:102-110 | `ShouldIncludeMovie`, `ShouldIncludeEpisode` — 5-line size threshold checks | Rung 5: fine, used for filtering. | Keep. | 0 |
| 84 | quality/scoring.go:112-150 | `ResolutionToString`, `SourceToString`, `AudioToString` — 40 lines of switch-based string converters | Rung 1: duplicates the `String()` methods on `Source`, `Resolution`, `AudioCodec` types. See #78. | Delete. Use type methods. | ~38 |
| 85 | quality/scoring.go:152-180 | `CodecToString` — 30-line codec extractor using the hand-rolled `contains` function | Rung 2: uses the reinvented `contains` (finding #1). Also, `CodecToString` takes a full filename but only does simple substring checks — it doesn't strip release markers first, so "BluRay.x264-GROUP" would match "x264" correctly but "x264-GROUP" wouldn't match "AV1" if AV1 appeared elsewhere. | Fix to use `strings.Contains`. Consider whether codec extraction belongs in quality package at all (naming package already strips codecs). | ~5 |
| 86 | quality/scoring.go:182-200 | `contains`, `stringContains`, `indexOfSubstring`, `toLower` — SEE FINDING #1 | Rung 2: already covered. | Delete. | ~30 |

## Needs Deeper Research

| # | Finding | What to Investigate |
|---|---------|---------------------|
| R1 | `KnownReleaseGroups` (10,992 entries) vs `knownReleaseGroups` (~80 entries) | Run both lists against a representative sample of 10,000+ real user filenames. Measure: (a) how many filenames have release groups caught by the 10k list but missed by the 80-entry list, (b) how many false positives each list produces. If the 80-entry list catches >95% of real release groups with fewer false positives, delete `blacklist.go`. The 10k list includes thousands of defunct scene groups from srrDB that will never appear in modern filenames. |
| R2 | `advanced.go` regex list vs `naming.go` regex list | Map which callers use `advReleasePatterns` vs `releasePatterns`. Check if any code path uses both. The lists are ~80% identical. Merging them into one package-level `var releasePatterns` would eliminate ~150 lines of init() code. Need to verify the extra patterns in `advReleasePatterns` (HDR variants, streaming platforms, commentary markers, bracketed content, bit depth) don't break `stripReleaseMarkers` behavior. |
| R3 | `StripReleaseGroup` vs `stripReleaseMarkers` | Trace all callers: `StripReleaseGroup` is called by `CleanMovieName`, `NormalizeName`, and `IsGarbageTitle`. `stripReleaseMarkers` is called by `parseMovieSimple`, `parseTVShowFromBaseName`, `ParseTVSeasonPackNameVerbose`, and `collectStrippedTokens`. Determine if `stripReleaseMarkers` can replace `StripReleaseGroup` for all callers, or if the abbreviation preservation in `StripReleaseGroup` is needed for specific cases. |
| R4 | `titleCaseWithOrdinals` external dependency | `golang.org/x/text/cases` is used only in `advanced.go:titleCaseWithOrdinals`. Check `go.mod` to see if `golang.org/x/text` is used elsewhere in the project. If not, replacing `cases.Title` with a simple `strings.Title` (deprecated but functional) or a hand-rolled title-caser would remove an external dependency. |
| R5 | `ParseFromPath` vs `ExtractMetadata` parent-dir walking | Both functions walk up parent directories looking for quality markers. `ExtractMetadata` walks 2 levels, `ParseFromPath` walks 1 level. Check if `ParseFromPath` has callers outside the quality package. If not, delete it and consolidate into `ExtractMetadata`. |
| R6 | `extractEpisodeInfo` callers | Search codebase for callers of `extractEpisodeInfo`. If none exist (all callers use `findEpisodeMatch` directly), delete the function. |
| R7 | `SimilarityRatio` / `levenshteinDistance` callers | Search for callers of `SimilarityRatio`. If it's only used for fuzzy dedup and a simpler algorithm (Jaccard, token overlap) would suffice, replace with a 5-line token-overlap function. |
