# Pipeline Ponytail Audit

## Findings

| Rank | File:Line | What | Why | Fix | Lines Saved |
|------|-----------|------|-----|-----|-------------|
| 1 | 7 files | Video extension map duplicated 7× (cleanup.go:13, dedup.go:16, organizer.go:574/col, organizer.go:625/find, scanner.go:430, watcher.go:175, pv.go uses its own) | Each map is slightly different (some include subtitles, `.flv`, `.webm`). No single source of truth. | One `var VideoExtensions = map[string]bool{...}` in a shared package (e.g. `internal/media`). All 7 sites import it. | ~60 |
| 2 | organizer.go:230-370, 372-510 | `OrganizeMovie` and `OrganizeMovieWithParsed` are ~90% identical (140 lines each). Same for `OrganizeTVEpisode`/`OrganizeTVWithParsed` (~180 lines each). | Only difference: one parses from path, the other takes pre-parsed info. | Single `organizeMovie(movie naming.MovieInfo, ...)` and `organizeTVEpisode(tv naming.TVShowInfo, ...)`. Callers parse first. | ~300 |
| 3 | transfer.go:24,27, transfer.go:89 | `ErrChecksumMismatch` and `ErrTransferFailed` sentinels never referenced. `TransferResult.Checksum` never populated. `TransferOptions.Checksum` field exists but no backend implements checksum verification. | Dead code. Speculative feature. | Delete `ErrChecksumMismatch`, `ErrTransferFailed`, `TransferResult.Checksum`, `TransferOptions.Checksum`. | ~10 |
| 4 | types.go:12-14 | `OrphanChecker` interface with one method, one implementation. | Interface with one impl = YAGNI. Forces `isNilOrphanChecker` reflection hack in periodic.go:88-98. | Delete interface. Use concrete type directly. Delete `isNilOrphanChecker` and its `reflect` import. | ~15 |
| 5 | transfer.go:148-155 | `MustNew` is deprecated per its own doc comment. | Dead code marked for removal. | Delete. | ~7 |
| 6 | organizer.go:197-210, 212-225, 227-233 | `Pause()`/`Resume()`/`IsPaused()` + `paused` field + `pauseMu` — never checked in any organize method. | Dead code. No organize flow calls `IsPaused()`. | Delete all 3 methods, `paused` field, `pauseMu` field. | ~20 |
| 7 | organizer.go:235-258 | `shouldWaitForScan()`/`waitIfScanning()` — never called by any organize method. | Dead code. | Delete both methods. | ~25 |
| 8 | watcher.go:42-46, 55 | `WithRecursive` option function + `Option` type for a single option that always defaults to `true` and is never set to `false`. | Functional options pattern for one boolean. Over-engineered. | Remove `Option`, `WithRecursive`, `opts` param from `NewWatcher`. Hardcode `recursive: true`. | ~10 |
| 9 | scanner.go:437-442 | `parseInt(s string)` wraps `fmt.Sscanf`. | `strconv.Atoi` is stdlib, simpler, faster. | Replace with `strconv.Atoi`. Delete `parseInt`. | ~5 |
| 10 | pv.go:237-248, rsync.go:218-229 | `scanPVOutput` and `scanRsyncOutput` are identical `bufio.SplitFunc` implementations. | Copy-paste. | One shared `scanLineOutput` in transfer package. | ~12 |
| 11 | health.go:177-213, native.go uses same pattern | `StatWithTimeout`, `OpenWithTimeout`, `RemoveWithTimeout` — same goroutine+channel+`time.After` pattern 3×. | Repeated boilerplate. | One generic helper: `func withTimeout[T any](timeout time.Duration, fn func() (T, error)) (T, error)`. Requires Go 1.18+. | ~30 |
| 12 | scanner.go:175-215, 217-257 | `scanPathWithProgress` duplicates `scanPathWithLibraryRoot` — only difference is progress callback every N files. | Copy-paste with one feature added. | Merge: add optional progress callback to `scanPathWithLibraryRoot`. | ~40 |
| 13 | scanner.go:99-115, 117-170 | `ScanLibraries` and `ScanWithOptions` have overlapping logic. `ScanLibraries` is a subset of `ScanWithOptions` without progress. | Two public APIs for same thing. | Delete `ScanLibraries`. `ScanWithOptions` covers it (pass nil `OnProgress`). | ~15 |
| 14 | ai_helper.go:48-52 | `tryParse` takes a `func() (*ai.Result, error)` closure parameter. Only two call sites, each wrapping a single method call. | Unnecessary abstraction. | Inline the two parse calls in `TryParse` and `TryParseWithContext`. Remove closure param. | ~5 |
| 15 | fallback.go:68 | `fmt.Printf("  ⚠️  %s failed, trying %s...\n", ...)` — side-effect print in library code. | Library code should not print to stdout. | Use injected logger or return structured error. | ~1 |
| 16 | reconcile.go:175-193 | `isDeterministicActivityFailure` matches on hardcoded error message substrings from other packages. | Fragile coupling. Error messages change → retry logic silently breaks. | Use explicit error types/sentinels from naming/organizer packages instead of string matching. | ~10 |
| 17 | organizer.go:68-72 | `Organizer` struct has 22 fields. 14 `With*` option functions (organizer.go:74-196). | God struct. Many optional fields (sonarrClient, jellyfinClient, db, syncService, pluginClient, playbackLocks, deferredQueue) that are nil in most paths. | Split into required core + optional `OrganizerExtensions` struct. Or accept that god structs happen in Go and leave it. | ~50 |
| 18 | transfer.go:58-107 | `TransferOptions` has 14 fields. `Checksum` is dead (see #3). `Progress` only used by native/pv. `DeletePartial` only used by native/pv. `SkipHealthCheck` is internal orchestration detail. | Bloated config struct. | Remove `Checksum`. Consider removing `Progress` (callers can wrap). | ~5 |
| 19 | health.go:44-141 | `CheckDiskHealthDetailed` implements timeouts via goroutines+channels+context for stat/statfs/write probe. | Go's `os.Stat`/`syscall.Statfs`/`os.Create` don't support context cancellation, so goroutine timeout is the only way to bound hung syscalls. Justified but verbose. | Keep. This is the minimum that works for hung-disk protection. | 0 |
| 20 | organizer.go:574-590, 625-641 | `collectVideoFiles` and `findExistingMediaFile` each define their own inline video extension map. | Duplication of #1 within same file. | Use shared constant (see #1). | ~15 |
| 21 | organizer.go:443-448 | `languageSuffixes` global var — 30+ language codes as a slice. | Could be a map for O(1) lookup. But slice is fine for this size. Minor. | Keep or convert to `map[string]bool`. | 0 |
| 22 | transfer.go:130-146 | `New()` factory builds fallback chain with `exec.LookPath` at construction time. If rsync/pv are installed later, they won't be picked up until restart. | Acceptable for a daemon. | Keep. | 0 |
| 23 | periodic.go:88-98 | `isNilOrphanChecker` uses `reflect.ValueOf` + `v.IsNil()` to detect nil interface values. | Go gotcha workaround. Would vanish if #4 is applied. | Delete with #4. | ~10 |
| 24 | scanner.go:28-30 | `progressReportInterval = 10` constant used once. | Could be inline. Trivial. | Keep or inline. | 0 |
| 25 | organizer.go:42-44 | `sync` import used only for `sync.RWMutex` (pauseMu) which is dead code (see #6). | Import becomes unused after #6 fix. | Removed automatically. | 1 |

## Needs Deeper Research

| # | Finding | Why cross-package |
|---|---------|-------------------|
| C1 | `isVideoFile` (scanner.go:430) vs `Handler.IsMediaFile` (watcher.go:30) — two different video-file detection paths. Scanner uses its own `isVideoFile`; watcher.Handler implementations may use a different `IsMediaFile`. | Need to verify all `Handler` implementations use the same extension set. If they differ, files could be watched but not scanned, or vice versa. |
| C2 | `processFile` in scanner.go (~200 lines) is the core parsing pipeline. It calls into `naming`, `quality`, `database`, `ai` packages. The parse-then-AI-then-save flow has 4 stages with complex branching. | This function is the integration point for 5 packages. Refactoring it requires understanding all downstream consumers. May be worth extracting a `FileProcessor` type but that's a bigger refactor. |
| C3 | `Transferer` interface has 4 backends + 1 wrapper. `FallbackTransferer` + `VolumeLimitedTransferer` are decorators. The `New()` factory in transfer.go:130 decides the chain. | Verify that `VolumeLimitedTransferer` is actually used in production paths. If it's only used in tests or one edge case, the mount-root detection logic (volume_limiter.go:88-115) may be dead code. |
| C4 | `reconcileActivity` in reconcile.go reads JSONL activity files written by the activity package. The retry logic depends on `activity.Entry` fields like `Deterministic`, `SourceMtime`, `Error`. | Verify the activity package actually writes these fields. If any are never populated, the corresponding retry branches are dead code. |
| C5 | `checkPlaybackSafetyWithOp` in organizer.go:260-300 has two independent playback detection paths: webhook locks (`playbackLocks`) and API polling (`jellyfinClient`). Both can be nil. | Verify both paths are actually used in production. If only one is configured, the other branch + its fields are dead weight. |
