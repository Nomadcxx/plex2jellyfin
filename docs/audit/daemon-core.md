# Daemon Core Ponytail Audit

## Findings

| Rank | File:Line | What | Why | Fix | Lines Saved |
|------|-----------|------|-----|-----|-------------|
| 1 | daemon.go:1-88 | Entire file: `Daemon` struct, `NewDaemon`, `Start`, `Stop`, `Run` | Dead code. Zero callers anywhere in the codebase. `NewDaemon` only referenced in its own file. `Start`/`Run` never invoked. The real daemon lifecycle is in `cmd/plex2jellyfin-daemon/main.go`. | Delete the file. | ~80 |
| 2 | handler.go:1297-1335 | `removeNonVideoContents` method | Dead code. Defined but never called anywhere. | Delete the method. | ~38 |
| 3 | ipc/frame_ring.go:12 | `cursor int` field on `FrameRing` | Dead field. `Append` uses slice append+truncate, never reads or writes `cursor`. Vestigial from an earlier ring-buffer design. | Delete the field. | 1 |
| 4 | ratelimit.go:59-61 | `Caps()` method on `AIRateLimiter` | Dead code. Zero callers. | Delete the method. | 3 |
| 5 | ratelimit.go:44-48 | `Record()` method on `AIRateLimiter` | Dead code. Only `RecordAndReport()` is called. `Record()` exists as a simpler variant that nothing uses. | Delete the method. | 5 |
| 6 | classify.go:19-20 | `ChangeStructural` constant | "Reserved for future use" per the comment. YAGNI. Never referenced in any classification logic. | Delete the constant. | 2 |
| 7 | handler.go:130-131 | `Stats` struct uses `sync.RWMutex` for four int64 counters and a `time.Time` | Over-engineered. `sync/atomic.Int64` + `atomic.Pointer[time.Time]` would be simpler, faster, and remove the `mu` field + Lock/Unlock boilerplate in `RecordMovie`, `RecordTV`, `RecordError`, `Snapshot`. | Replace with `atomic.Int64` fields. | ~15 |
| 8 | handler.go:148-155 | `StatsSnapshot` type | Unnecessary abstraction. Only used as return from `Snapshot()`. The caller (`server.go:handleMetrics`) destructures it immediately. Could just return the fields directly or embed in `MetricsResponse`. | Inline into `MetricsResponse` or return bare values. | 7 |
| 9 | enhancelog.go:47-49 | `EnhanceLogger.maxSize`, `maxBackups` struct fields | Hardcoded to 10MB/3 in `NewEnhanceLogger`, never configurable, never change. Fields exist to hold constants. | Make package-level consts. | 2 |
| 10 | handler.go:86 | `pendingAICap` field hardcoded to 100 | Never configurable, never changes. | Make a const. | 1 |
| 11 | handler.go:80-81 | `transientRetryDelay`, `seasonPackEventSuppressWindow` package vars | Never mutated. Should be `const` (or `const`-like unexported vars, but Go doesn't have duration consts — still, they're effectively constants). | Document as effectively-const; no code change needed but flag as pattern. | 0 |
| 12 | handler.go:239-245 | `allWatchPaths()` allocates a new slice every call | Called from `HandleFileEvent` (hot path). Pre-compute once in `NewMediaHandler`. | Pre-compute in constructor. | ~3 |
| 13 | handler.go:268-278 | `isInsideLibrary` rebuilds prefix checks every call | `filepath.Clean` + string concat on every invocation. Pre-compute cleaned paths with trailing separator in constructor. | Pre-compute `cleanLibPrefixes` slice. | ~4 |
| 14 | handler.go:1380-1387 | `cleanupSourceDir` rebuilds `rootSet` map every call | Called after every successful organize. Map allocation + population on every success. | Pre-compute `watchRootSet` in constructor. | ~6 |
| 15 | handler.go:252-264 | `getSourceHint` O(n) linear scan every call | Called in `processFile` hot path. Could be a pre-built map or prefix tree. | Pre-build `map[string]naming.SourceHint` keyed by watch root. | ~4 |
| 16 | handler.go:101-102 | `loggedErrors` map grows unbounded | No eviction. Long-running daemon accumulates every distinct (path, error) pair forever. Memory leak. | Add LRU eviction or TTL-based cleanup goroutine. | 0 (add ~10 lines of eviction) |
| 17 | handler.go:98-99 | `transientWarned` map grows unbounded | Never cleaned. Every distinct path that triggers a transient defer stays in the map forever. | Clear entries after successful process or on timer. | 0 (add ~5 lines of cleanup) |
| 18 | handler.go:1206-1232 | `sendNotificationsWithTracking` returns `sonarrNotified, radarrNotified` derived from media type, not actual notifier results | Misleading name. The "tracking" is just `mediaType == TV → sonarr=true, mediaType == Movie → radarr=true`. Doesn't check whether Sonarr/Radarr notifiers are actually configured or succeeded. | Rename to `sendNotifications` and derive booleans at call site, or actually check notifier config. | ~4 |
| 19 | handler.go:1476-1530 | `HandleJellyfinWebhookEvent` — `EventItemAdded` and `EventItemUpdated` cases are ~90% identical | The only difference is `GetUnresolvedDecisionByTargetPath` vs `GetDecisionByTargetPath` and `UpdateOutcome` vs `UpgradeOutcome`. 40 lines of duplication. | Extract shared logic into a helper. | ~20 |
| 20 | handler.go:1590-1593 | `replayDeferredOperation` checks `h.logger != nil` twice | Guard clause at line 1591 checks it, then line 1594 checks it again before logging. | Remove the second check. | 1 |
| 21 | handler.go:730-870 | `processFile` TV and Movie branches share ~80% structural similarity | Parse→DB update→AI queue→organize flow is identical. The branches differ only in which parser/organizer is called. | Extract a `processMediaFile(path, mediaType, parser, organizer)` helper. | ~50 |
| 22 | handler.go:1700-1790 | `organizeWithRegexFallback` duplicates the regex path from `processFile` | ~90 lines that mirror the TV/Movie branches in `processFile` almost verbatim. | Call `processFile` directly (it already handles the regex path). | ~90 |
| 23 | handler.go:68-76 | `aiRetryBackoff` allocates a new slice on every call | The backoff schedule is static. | Make a package-level `var aiBackoffSchedule = [...]time.Duration{...}` | 3 |
| 24 | ipc/op_registry.go:107-113 | `List()` uses O(n²) bubble sort | `sort.Slice` is in stdlib. Bubble sort on a list that's typically <10 items is harmless but unnecessary cleverness. | `sort.Slice(out, func(i, j int) bool { return out[i].StartedAt > out[j].StartedAt })` | 4 |
| 25 | ratelimit.go:12-18 | `AIRateLimiter` struct has `hourlyReset`, `dailyReset` as mutable `time.Time` fields | The reset times are computed in `Allow()` on every call. The fields exist only to be compared against `now`. Could compute windows from a single `startTime` + `time.Now()` without storing reset deadlines. | Compute windows arithmetically: `now.Truncate(time.Hour).Add(time.Hour)` instead of stored deadlines. | ~4 |
| 26 | handler.go:1558-1590 | `runJellyfinVerificationPass` does O(n) individual DB queries | Reads 200 activity entries, then calls `GetJellyfinItemByPath` for each. Could batch-query all target paths in one DB call. | Batch query. | 0 (cross-package: needs DB method) |
| 27 | reload/ai_reloadable.go:8-10 | `AIReconfigurer` interface with one implementation | Interface exists solely for `aiReloadable`. The `Reloadable` interface already provides the abstraction; `AIReconfigurer` is an extra layer. | Make `NewAIReloadable` take a `func(config.AIConfig) error` directly. | 3 |
| 28 | reload/scanner_reloadable.go:8-10 | `WatchPathReplacer` interface with one implementation | Same pattern as #27. Interface for a single concrete type. | Take `func([]string) error` directly. | 3 |

## Needs Deeper Research

| # | Finding | Why cross-package |
|---|---------|-------------------|
| 1 | `ReadFlaggedForReview` in `enhancelog.go` | Standalone function that reads the enhance log for the CLI `review` command. Lives in the daemon package but is only called from `cmd/plex2jellyfin`. Should move to a shared package or the CLI layer. |
| 2 | `tvOrganizer`/`movieOrganizer` duplication in `NewMediaHandler` | Two separate `organizer.Organizer` instances created with nearly identical option chains. The `organizer` package could accept a media type parameter instead of requiring two instances. Would eliminate ~40 lines of construction code and the `wrapTransferer` closure. |
| 3 | `processFile` regex path vs `organizeWithRegexFallback` | `organizeWithRegexFallback` is a near-verbatim copy of the regex branch in `processFile`. Could `processFile` be called directly for fallback items? Need to verify that `processFile`'s debounce/pending/transientRetry maps don't interfere with fallback execution. |
| 4 | `runJellyfinVerificationPass` O(n) queries | Needs a `GetJellyfinItemsByPaths([]string)` batch method on `database.MediaDB`. Currently does N individual `GetJellyfinItemByPath` calls. |
| 5 | `Stats` atomic migration | `Stats` is accessed from `handleMetrics` HTTP handler (read) and `processFile`/`applyAIResult` (write). `sync/atomic` would remove the `sync.RWMutex` but requires verifying no caller depends on mutual exclusion across multiple fields. |
| 6 | `CmdDupScan`, `CmdVerifyFlagged`, `CmdTask*` commands in protocol.go | These are registered in `cmd/plex2jellyfin-daemon/main.go` but their handlers live outside the daemon package. The protocol constants are fine, but the command set is sprawling (30+ commands). Consider whether some could be consolidated (e.g. `TASK_GET` + `TASKS_BULK` + `TASK_GROUP`). |
