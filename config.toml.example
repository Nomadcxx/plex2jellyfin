# Plex2Jellyfin Configuration
# Location: ~/.config/plex2jellyfin/config.toml

# Directories to watch for new media files
[watch]
movies = ["/path/to/downloads/movies"]
tv = ["/path/to/downloads/tv"]

# Destination Jellyfin library directories
[libraries]
movies = ["/path/to/jellyfin/Movies"]
tv = ["/path/to/jellyfin/TV Shows"]

# Daemon settings
[daemon]
enabled = true
scan_frequency = "5m"
health_addr = ":8686"

# General options
[options]
dry_run = false
verify_checksums = false
delete_source = true

# Sonarr integration (for TV shows)
# Get API key from: Sonarr -> Settings -> General -> API Key
[sonarr]
enabled = false
url = "http://localhost:8989"
api_key = ""
notify_on_import = true

# Radarr integration (for movies)
# Get API key from: Radarr -> Settings -> General -> API Key
[radarr]
enabled = false
url = "http://localhost:7878"
api_key = ""
notify_on_import = true

# File permissions (optional)
# Set ownership and permissions for transferred files.
# Useful when Jellyfin runs as a different user than the download client.
# Leave user/group empty to preserve source ownership.
[permissions]
# user = "jellyfin"      # Username or numeric UID
# group = "jellyfin"     # Group name or numeric GID
# file_mode = "0644"     # File permissions (rw-r--r--)
# dir_mode = "0755"      # Directory permissions (rwxr-xr-x)

[jellyfin]
# enabled = true
# url = "http://localhost:8096"
# api_key = "..."
# webhook_secret = "..."

# Path mappings translate between Jellyfin's view of a media file (typically
# a container-internal path) and the daemon's view (the host filesystem
# path). Required whenever Jellyfin runs in a container with bind mounts
# whose roots differ from the daemon's. Without these, the post-organize
# feedback loop (sweeper + webhook) cannot correlate Jellyfin items with
# parse_decisions rows and every row will eventually be auto-labeled FAIL.
# Mappings are applied longest-prefix first.
#
# [[jellyfin.path_mappings]]
# jellyfin = "/tv5"
# daemon   = "/mnt/STORAGE5/TVSHOWS"
#
# [[jellyfin.path_mappings]]
# jellyfin = "/movies"
# daemon   = "/mnt/STORAGE2/MOVIES"
